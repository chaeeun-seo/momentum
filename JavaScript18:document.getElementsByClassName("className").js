// Searching For Elements

// document.getElementsByClassName("className");
// to select many elements at the same time using class 
// we can get array of elements whose classname matches

const hellos = document.getElementsByClassName("hello");

console.log(hellos);
